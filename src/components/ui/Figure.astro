---
import { Image } from "astro:assets";
const { src, alt, caption } = Astro.props;
---

<figure class="my-10">
  <Image src={src} alt={alt} class="rounded-lg" />
  {
    caption && (
      <figcaption class="mt-2 text-sm text-muted text-center">{caption}</figcaption>
    )
  }
</figure>
